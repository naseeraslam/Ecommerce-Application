<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <%= link_to 'Square Mall',root_path,class:"navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to 'DashBoard',root_path,class:"nav-link px-3" %>
        </li>
        <% if user_signed_in? == true %>
          <li class="nav-item">
            <%= link_to 'My Products',products_path,class:"nav-link px-3" %>
          </li>
        <%end%>
        <li class="nav-item">
          <%= link_to "Cart", cart_dashboard_index_path,class:"nav-link px-3"%>
        </li>
        <li class="nav-item">
          <%= yield :search %>
        </li>
      </ul>
      <div class="navbar-nav nav-item px-5">
        <% if user_signed_in?%>
          <li class="nav-item ps-5">
            <%= button_to "Sign out", destroy_user_session_path, method: :delete, class:"nav-link px-3 btn btn-dark" %>
          </li>
        <%else%>
          <%= link_to "Sign In", user_session_path, method: :get, class:"nav-link px-3"   %>
        <%end%>
      </div>
      <div class="navbar-nav nav-item">
        <%  if current_user && current_user.avatar.attached? %>
          <%= link_to(image_tag(current_user.avatar, class:'profile-icon mr-5', title:'View or Edit Profile'), edit_user_registration_path) %>
        <% elsif current_user %>
          <%= link_to edit_user_registration_path do %>
            <i class='fas fa-user-circle icon-links'></i>
          <% end %>
        <%end%>
      </div>
    </div>
  </div>
</nav>
